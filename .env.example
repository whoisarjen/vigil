# Database (Neon serverless PostgreSQL)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Auth (nuxt-auth-utils)
NUXT_SESSION_PASSWORD=your-secret-at-least-32-characters-long
NUXT_OAUTH_GOOGLE_CLIENT_ID=your-google-client-id
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Cron Security
# Used by Vercel cron and external cron services to authenticate requests
# Generate with: openssl rand -hex 16
CRON_SECRET=your-cron-secret

# External Cron (recommended for frequent checks)
# Vercel free tier only supports daily cron. For 15-minute checks,
# use a free external service like cron-job.org to call:
# GET https://your-domain.vercel.app/api/cron/check
# with header: Authorization: Bearer <CRON_SECRET>
